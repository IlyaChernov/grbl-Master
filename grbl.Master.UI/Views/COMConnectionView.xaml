<UserControl x:Class="grbl.Master.UI.Views.COMConnectionView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="26" d:DesignWidth="256" mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80" />
            <ColumnDefinition Width="80" />
            <ColumnDefinition Width="26" />
            <ColumnDefinition Width="70" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ComboBox x:Name="ComPorts" Margin="1" SelectedItem="{Binding Path=SelectedComPort}" IsEnabled="{Binding CanChangePortBaud}" />
        <ComboBox Grid.Column="1" x:Name="BaudRates" Margin="1" SelectedItem="{Binding Path=SelectedBaudRate}" IsEnabled="{Binding CanChangePortBaud}" />
        <Button x:Name="ReloadComPorts" Grid.Column="2" Margin="1" Padding="0">Rel</Button>
        <Button x:Name="Connect" Grid.Column="3" Margin="1" Padding="0" Content="{Binding ConnectButtonCaption}" />
    </Grid>
</UserControl>