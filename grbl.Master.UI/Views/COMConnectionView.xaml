<UserControl x:Class="grbl.Master.UI.Views.COMConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:grbl.Master.UI.Views"        
             mc:Ignorable="d" 
             d:DesignHeight="26" d:DesignWidth="195">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="26"/>
            <ColumnDefinition Width="70"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ComboBox x:Name="ComPorts" Margin="1" SelectedItem="{Binding Path=SelectedComPort, Mode=OneWayToSource}" />
        <Button  x:Name="ReloadComPorts" Grid.Column="1" Margin="1" Padding="0" >Rel</Button>
        <Button x:Name="Connect" Content="{Binding ConnectButtonCaption}" Grid.Column="2" Margin="1" Padding="0" />

    </Grid>
</UserControl>
